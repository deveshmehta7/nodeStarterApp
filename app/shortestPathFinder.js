/*jslint node: true, nomen: true, plusplus: true*/
"use strict";
var config = require('./config/config'),
    utils = require('../utils/utilityService');


/**
 *  Converts the input that is required to system
 * @param input
 */
function transformInput(input) {
  var transformedInput;

  //input
  return transformedInput;
}


function pathMaker(input) {
  var paths = [];

}


/**
 * Finds the best optimun path for the given input
 * @param input
 */
exports.shortestPath = function (input) {

  var data = transformInput(input);
  var pathmaker = pathMaker(input);
};